mkdir ~/share/
mkdir ~/share/cpan/
mkdir ~/dev/
mkdir ~/src/
mkdir ~/tmp/
mkdir ~/local/
ln -s `pwd`/local/bin ~/local/bin

sudo aptitude install -y psmisc python-dev rcs rsync ruby scons sqlite3 spidermonkey-bin strace subversion sudo swig tcpdump telnet traceroute libpng-dev libjpeg-dev libgif-dev unison unzip zip valgrind wamerican whois zlib1g-dev
sudo aptitude install -y git-core screen zsh curl w3m wget nkf  autotools-dev gcc binutils-dev bsdutils tree bvi emacs23-nox vim lv doxygen ncftp lftp gdb gnupg2 ipython ngrep irb irssi-dev irssi-scripts aspell mecab locate lsof memcached nasm nmap libxml2-dev pkg-config libreadline-dev make munin
sudo aptitude install bzr cvs gauche-dev graphviz-dev rubygems ruby-full mutt libruby-json

wget http://openlab.jp/skk/skk/dic/SKK-JISYO.L --output-document ~/share/SKK-JISYO.L

cd ~/share/
git clone git://github.com/tokuhirom/tokuhirom-dotfiles.git dotfiles
cd dotfiles/
perl setup.pl|sh

sudo -H cpan local::lib
sudo -H rm -rf ~/.cpan/


# chsh.

# re-run zsh

o conf urllist http://cpan.yahoo.com/
o conf prerequisites_policy follow
o conf commit
install Bundle::CPAN
install CPAN::SQLite
o conf use_sqlite 1
o conf commit
reload index
install Module::Install

# use task-belike-tokuhirom
cd ~/dev
git clone git://github.com/tokuhirom/task-belike-tokuhirom.git
cd task-belike-tokuhirom
perl Makefile.PL
perl -e 'use YAML; print "cpan -i " . join " ", keys %{YAML::LoadFile("META.yml")->{requires}}, "\n"'|zsh

sudo ln -s /etc/service/ /servic

Ubuntu specific
==============

if you got "Looking up ..." is slow, try this:
https://help.ubuntu.com/community/WebBrowsingSlowIPv6IPv4

sudo apt-get  build-dep w3m
sudo aptitude purge kasumi

use ubuntu as X11 Client
------------------------

sudo aptitude install urxvt
sudo aptitude install scim-uim uim-skk uim-gtk2.0 uim-xim

remap key
---------
check keycode by `xev`.

and edit ~/.Xmodmap such as:

    keycode 33 = grave tilde

OSX
---

install some native softwares
- firefox
- keyremap4macbook
- aquaskk
- macports

instal ports
- git-core +svn
- wget
- coreutils

nginx
---
wget http://nginx.org/download/nginx-0.8.30.tar.gz
